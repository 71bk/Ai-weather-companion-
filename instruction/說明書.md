🧭 Routing / Views 設計
1. HomeView.vue – 天氣查詢主頁

功能：

地區搜尋（縣市 / 鄉鎮）

顯示目前與未來 36 小時天氣摘要

顯示 AI 總結按鈕（呼叫 AI，輸出 2~4 句解讀）

顯示近期查詢紀錄（從 Pinia 讀）

主要用到的元件：

WeatherSearchBar.vue

WeatherSummaryCard.vue

HourlyForecastList.vue

AISummaryButton.vue

AIInsightBox.vue

2. AIAssistantView.vue – AI Chat 天氣助理頁

功能：

類似 Chat UI：左邊是歷史對話，底部輸入框

使用者可以問：「今天適合曬棉被嗎？」之類的問題

前端會將目前 weatherStore 中的天氣資料 + 使用者問題組合成 prompt，送給 AI

主要用到的元件：

AIChatPanel.vue

WeatherSummaryCard.vue（側邊顯示目前地區摘要）

3. ChartsView.vue – 圖表 + AI 趨勢解讀頁

功能：

顯示溫度折線圖：TemperatureChart.vue

顯示降雨機率折線圖：RainProbabilityChart.vue

提供「讓 AI 解讀這些圖表」按鈕 → 呼叫 AI，將圖表對應的數據傳給 AI

主要用到的元件：

TemperatureChart.vue

RainProbabilityChart.vue

AISummaryButton.vue

AIInsightBox.vue

4. AboutView.vue – 關於頁

內容建議：

專案目的：練習 API & AI 整合的前端作品

使用技術說明

資料來源（天氣 API / AI API）

你的個人簡短介紹

🧩 Components 設計
Layout
AppShell.vue

負責主框架：Header + <router-view /> + Footer

可在此處放置主背景、Container 寬度等

AppHeader.vue

Logo、專案名稱（AI Weather Companion）

主導航：Home / AI Assistant / Charts / About

Weather Components
WeatherSearchBar.vue

Props：

modelValue（目前選定地區，可用 v-model 綁定）

Emit：

update:modelValue – 使用者選地區

search – 使用者按下搜尋按鈕

UI：

一個輸入框 + 自動完成 / 下拉（可用 Element Plus 的 el-select / el-autocomplete）

一個搜尋按鈕

WeatherSummaryCard.vue

顯示目前主要資訊：

地區名稱

天氣現象（晴、多雲、雨）

氣溫區間

體感描述

HourlyForecastList.vue

接受 forecasts 陣列 props

使用 v-for 繪製 ForecastItemCard.vue

AI Components
AIChatPanel.vue

功能：

顯示聊天訊息列表（user / assistant）

底部輸入框 + 送出按鈕

Loading 狀態顯示

資料來源：

從 aiStore 讀取 / 更新 messages

AISummaryButton.vue

用途：

放在 Home / Charts 頁面，提供「一鍵 AI 總結」的操作

行為：

點擊後呼叫 aiStore.fetchSummary()，並將需要的資料（當前天氣或 chart data）一併傳入

AIInsightBox.vue

用途：

顯示 AI 回覆的摘要內容（例如天氣趨勢、建議）

Charts Components
TemperatureChart.vue

Props：

data – Array<{ time: string; temperature: number }>

行為：

使用 ECharts / Recharts 繪製折線圖

RainProbabilityChart.vue

Props：

data – Array<{ time: string; probability: number }>

🗃 Stores (Pinia)
weatherStore.ts

State：

currentLocation：目前查詢地區

currentWeather：目前天氣資料

forecast36h：36 小時預報資料

history：過去查詢紀錄陣列

Actions：

fetchWeather(location)：呼叫 weatherApi.getWeather(location)

setLocation(location)：更新目前地區

loadFromCache()：從 localStorage 還原歷史紀錄（可選）

aiStore.ts

State：

messages：Chat 訊息列表

isLoading：是否正在等待 AI 回覆

lastSummary：最近一次 AI summary 的內容

Actions：

askQuestion(question, weatherData)：組 prompt → 呼叫 aiApi.askWeatherAssistant(...)

summarizeWeather(weatherData)：專門給一鍵總結用

summarizeCharts(chartData)：專門給圖表解讀用

🌐 API Layer
weatherApi.ts

負責封裝所有與天氣有關的 HTTP 請求。

import axios from 'axios'

const client = axios.create({
  baseURL: import.meta.env.VITE_WEATHER_API_BASE_URL,
})

export async function getWeatherByLocation(location: string) {
  const res = await client.get('/forecast', { params: { location } })
  return res.data
}

aiApi.ts

負責呼叫 AI API。

import axios from 'axios'

const client = axios.create({
  baseURL: import.meta.env.VITE_AI_API_BASE_URL,
  headers: {
    Authorization: `Bearer ${import.meta.env.VITE_AI_API_KEY}`,
  },
})

export async function askWeatherAssistant(payload: {
  question: string
  weatherData: unknown
}) {
  const res = await client.post('/weather-assistant', payload)
  return res.data
}

🔐 Environment Variables

在專案根目錄建立 .env.example：

VITE_WEATHER_API_BASE_URL=https://api.example.com/weather
VITE_AI_API_BASE_URL=https://api.example.com/ai
VITE_AI_API_KEY=your_api_key_here


真正的 .env 不要 commit 到 GitHub。

✅ 開發流程建議

先完成 Routing + Layout（AppShell + Header + Footer）

再完成 weatherStore + weatherApi + HomeView 的天氣查詢

加上 ChartsView + 圖表元件

最後整合 aiStore + aiApi + AI Components（AIChatPanel / AISummaryButton / AIInsightBox）